# FlowOSfr

backend commands~
ts-node is a TypeScript execution environment for Node.js. It allows you to run TypeScript code directly in Node.js without the need to compile it first.
Install Typescript & ts-node -- npm install --save-dev typescript @types/express ts-node-dev

scripts in package.json:

"dev": "tsx watch index.ts", >>>>>     tsx, a fast TypeScript/ESM runner.
"build": "tsc"               >>>>>     this will compile our typescript code 
